# FastAPI and Web Server
fastapi
uvicorn
pydantic
python-dotenv
python-multipart

# Redis Cache (graceful degradation – app works without Redis)
redis[hiredis]

# LLM Providers and Clients
groq
qdrant-client

# Machine Learning and Emotion Inference (CPU only)
# Install torch CPU-only via the dedicated PyTorch index in the Dockerfile
# to avoid pulling the massive CUDA wheels from PyPI.
torch
transformers
sentence-transformers
scikit-learn
joblib

# LangChain Core and Vector Store Integrations
langchain-core
langchain-community
langchain-qdrant
langchain-groq
langchain-classic

# Hugging Face Integration
huggingface-hub

# Authentication & Security
# passlib 1.7.4 is incompatible with bcrypt>=4.1 (removed __about__.__version__).
# Pin bcrypt<4.1 to avoid the AttributeError at import time.
python-jose[cryptography]
passlib[bcrypt]==1.7.4
bcrypt==4.3.0

# numpy – pin below 2.0 to avoid ABI breakage with torch/sklearn/transformers
numpy